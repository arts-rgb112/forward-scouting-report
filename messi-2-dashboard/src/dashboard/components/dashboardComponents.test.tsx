// @vitest-environment jsdom
import "@testing-library/jest-dom/vitest"; import { cleanup, fireEvent, render, screen } from "@testing-library/react"; import { afterEach, describe, expect, it, vi } from "vitest"; import MessiScoutingDashboard from "../MessiScoutingDashboard"; import { sampleMeta, samplePlayers } from "../../test/fixtures/players"; import { AssetImage } from "./AssetImage"; import { DashboardErrorBoundary } from "./DashboardErrorBoundary"; import { PlayerCardList } from "./PlayerCardList";
afterEach(() => { cleanup(); localStorage.clear(); });
describe("asset nullability", () => { it("uses initials without making a network image", () => { render(<AssetImage src={null} alt="Erling portrait" kind="face" fallbackLabel="Erling Haaland" width={48} height={48} />); expect(screen.getByRole("img", { name: "Erling portrait" })).toHaveTextContent("EH"); }); });
describe("dashboard contract UI", () => { it("uses server ranks and all six metrics", () => { render(<MessiScoutingDashboard players={samplePlayers} meta={sampleMeta} refreshing={false} onRefresh={vi.fn()} />); expect(screen.getAllByText("02").length).toBeGreaterThan(0); expect(screen.getAllByText("공간 지배").length).toBeGreaterThan(0); expect(screen.getAllByTitle("Diamond II, level 2").length).toBeGreaterThan(0); }); it("mobile cards expose six metrics without disclosure", () => { const { container } = render(<PlayerCardList players={samplePlayers.slice(0, 1)} comparedIds={new Set()} watchedIds={new Set()} onToggleCompare={vi.fn()} onToggleWatch={vi.fn()} />); expect(container.querySelectorAll("article [role='tooltip']")).toHaveLength(0); expect(screen.getByText("공간")).toBeInTheDocument(); }); });
describe("render failure containment", () => { it("shows a safe recovery action for a rendering failure", () => { const retry = vi.fn(); const Thrower = () => { throw new Error("test render failure"); }; const consoleError = vi.spyOn(console, "error").mockImplementation(() => undefined); render(<DashboardErrorBoundary resetKey={0} onReset={retry}><Thrower /></DashboardErrorBoundary>); expect(screen.getByRole("alert")).toHaveTextContent("Dashboard rendering error"); fireEvent.click(screen.getByRole("button", { name: "Try again" })); expect(retry).toHaveBeenCalledOnce(); consoleError.mockRestore(); }); });
