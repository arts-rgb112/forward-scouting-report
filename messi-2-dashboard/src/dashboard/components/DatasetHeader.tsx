import type { DatasetMeta } from "../types";
export function DatasetHeader({ meta, visibleCount, refreshing, onRefresh }: { meta: DatasetMeta; visibleCount: number; refreshing: boolean; onRefresh(): void }) { return <header className="mb-5 flex flex-col gap-4 border-b border-white/10 pb-5 xl:flex-row xl:items-end xl:justify-between"><div><div className="mb-2 flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-lg border border-lime-300/30 bg-lime-300/10 font-black text-lime-300">M</div><span className="text-[10px] font-bold uppercase tracking-[.28em] text-emerald-300">Forward Intelligence / 2.0</span></div><h1 className="text-2xl font-black sm:text-3xl">M.E.S.S.I. <span className="text-zinc-500">SCOUT INDEX</span></h1><p className="mt-1 text-xs text-zinc-500">Six-sector forward evaluation · generated {new Date(meta.generatedAt).toLocaleString()}</p>{meta.returned < meta.population && <p className="mt-2 text-xs text-amber-300">Partial dataset: {meta.returned.toLocaleString()} of {meta.population.toLocaleString()} players returned.</p>}</div><div className="flex items-stretch gap-2"><div className="grid grid-cols-4 gap-px overflow-hidden rounded-lg border border-white/10 bg-white/10 text-center">{[["Visible", visibleCount], ["Returned", meta.returned], ["Scope", meta.scope], ["Season", meta.season]].map(([label, value]) => <div key={label} className="bg-[#101415] px-3 py-2"><div className="text-[9px] uppercase text-zinc-500">{label}</div><div className="font-mono text-sm font-black text-lime-300">{value}</div></div>)}</div><button onClick={onRefresh} disabled={refreshing} className="min-h-11 rounded-lg border border-white/10 px-3 text-xs disabled:opacity-50">{refreshing ? "Refreshing…" : "Refresh"}</button></div></header>; }
