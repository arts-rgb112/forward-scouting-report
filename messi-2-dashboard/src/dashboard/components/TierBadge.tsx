import { tierConfig } from "../scoutingConfig"; import type { Tier } from "../types";
export function TierBadge({ tier, compact = false }: { tier: Tier; compact?: boolean }) { const config = tierConfig[tier.code]; return <span className={`inline-flex items-center justify-center gap-1 rounded border px-1.5 py-1 font-mono font-bold ${compact ? "text-[9px]" : "text-[10px]"} ${config.className}`} title={`${tier.label}, level ${tier.level}`}><span aria-hidden="true">{config.glyph}</span><span>{compact ? tier.code.slice(0, 3).toUpperCase() : tier.label}</span><span>Lv.{tier.level}</span></span>; }
