import { describe, expect, it } from "vitest"; import { MessiApiError } from "../api/errors"; import { sampleMeta, samplePlayers } from "../test/fixtures/players"; import { playersResourceReducer } from "./playersResourceState";
const payload = { players: samplePlayers, meta: sampleMeta };
describe("resource reducer", () => { it("ignores stale results", () => { const loading = playersResourceReducer({ type: "idle" }, { type: "start", requestId: 2 }); expect(playersResourceReducer(loading, { type: "resolve", requestId: 1, payload })).toEqual(loading); }); it("keeps stable data on refresh failure", () => { const refreshing = playersResourceReducer({ type: "success", payload }, { type: "start", requestId: 2, previous: payload }); const result = playersResourceReducer(refreshing, { type: "reject", requestId: 2, error: new MessiApiError("network", "offline") }); expect(result.type).toBe("error"); expect(result.type === "error" && result.previous).toBe(payload); }); });
