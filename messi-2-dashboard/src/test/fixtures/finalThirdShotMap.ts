export const finalThirdShotMapFixture = {
  schemaVersion: "1.0.0", chartTaxonomyVersion: "final-third-shot-map-v1",
  context: { playerId: 194165, idNamespace: "fotmob", season: "2025/2026", mode: "league", scope: 8, competition: null, depthBand: "front2" },
  data: { available: true, completeness: "partial", reason: null, gridVersion: "positional-6x5-v1", attackDirection: "left_to_right", includedDepths: [5, 6], qualityScale: { min: -0.5, neutral: 0, max: 0.5, version: "final-third-quality-v1" }, markerSizeScale: { min: 0, max: 1, version: "xg-natural-0-to-1-v1" }, goalMouthCoordinates: { version: "goal-mouth-v1", unit: "normalized", horizontalMin: 0, horizontalMax: 1, verticalMin: 0, verticalMax: 1, origin: "bottom_left_shooter_view", horizontalDirection: "shooter_left_to_right", verticalDirection: "ground_to_crossbar" },
    zones: [1, 2, 3, 4, 5].flatMap((lane) => [6, 5].map((depth) => ({ zoneId: `depth${depth}_lane${lane}`, depth, lane, shotsTotal: depth === 6 && lane === 3 ? 2 : 0, goals: depth === 6 && lane === 3 ? 1 : 0, conversionRatePct: depth === 6 && lane === 3 ? 50 : null, qualityScore: depth === 6 && lane === 3 ? .3 : null, qualityEligibleShots: depth === 6 && lane === 3 ? 1 : 0, state: depth === 6 && lane === 3 ? "partial" : "observed", reason: depth === 6 && lane === 3 ? "xgot_or_xg_unavailable_for_1_shots" : null, source: "player_season_shot_events", qualityFormulaVersion: "avg-xgot-minus-avg-xg-v1", fieldStates: { volume: { state: "observed", reason: null, source: "player_season_shot_events", formulaVersion: null }, conversionRatePct: depth === 6 && lane === 3 ? { state: "observed", reason: null, source: "player_season_shot_events", formulaVersion: "goals-divided-by-shots-v1" } : { state: "unavailable", reason: "no_attempts_in_zone", source: "player_season_shot_events", formulaVersion: null }, qualityScore: depth === 6 && lane === 3 ? { state: "partial", reason: "xgot_or_xg_unavailable_for_1_shots", source: "player_season_shot_events", formulaVersion: "avg-xgot-minus-avg-xg-v1" } : { state: "unavailable", reason: "no_attempts_in_zone", source: "player_season_shot_events", formulaVersion: "avg-xgot-minus-avg-xg-v1" } } }))).sort((a, b) => (b.depth - a.depth) || (a.lane - b.lane)),
    shots: [{ shotId: "goal-1", shotIdSource: "provider_event", zoneId: "depth6_lane3", pitchX: 90, pitchY: 50, xg: .2, xgot: .5, status: "goal", endpointAvailable: true, goalMouthY: 1.1, goalMouthZ: .5, endpointReason: null, source: "player_season_shot_events" }, { shotId: "blocked-1", shotIdSource: "snapshot_record", zoneId: "depth6_lane3", pitchX: 90, pitchY: 50, xg: 0, xgot: null, status: "blocked", endpointAvailable: false, goalMouthY: null, goalMouthZ: null, endpointReason: "blocked_has_no_goal_mouth_endpoint", source: "player_season_shot_events" }], endpointUnavailableCount: 1, endpointUnavailableShotIds: ["blocked-1"], partialCoverage: [{ zoneId: "depth6_lane3", shotId: null, field: "qualityScore", reason: "xgot_or_xg_unavailable_for_1_shots" }]
  }
} as const;
