import { useState } from "react"; import { PlayersResourceContainer } from "../dashboard/PlayersResourceContainer"; import { DashboardErrorBoundary } from "../dashboard/components/DashboardErrorBoundary"; import { StaticRoute } from "./StaticRoute";
export default function App() { const [resetKey, setResetKey] = useState(0); const routed = window.location.pathname !== "/"; return <DashboardErrorBoundary resetKey={resetKey} onReset={() => setResetKey((key) => key + 1)}>{routed ? <StaticRoute /> : <PlayersResourceContainer key={resetKey} />}</DashboardErrorBoundary>; }
