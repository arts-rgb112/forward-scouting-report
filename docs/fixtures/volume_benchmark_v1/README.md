# Volume benchmark v1 fixtures

- `success.json`: populated six-axis domestic 8-league benchmark.
- `unavailable.json`: player context exists but the benchmark source is unavailable.
- `observed_zero.json`: zero is observed data, while `null` is explicitly imputed/unavailable.

The numerical values are contract examples. Live values change when a verified cohort refreshes.
